[project]
name = "vqr-semantic-view-generator"
version = "0.1.0"
description = "Generate Semantic View YAMLs from VQR/SQL query analysis"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "snowflake-connector-python>=3.0.0",
]
